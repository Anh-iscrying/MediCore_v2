server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api/v1

spring:
  datasource:
    # URL lấy từ Supabase PostgreSQL
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # Tự động cập nhật bảng mới
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true

  # Cấu hình bảo mật JWT
  security:
    jwt:
      secret-key: ${JWT_SECRET}
      expiration: ${JWT_EXPIRATION:86400000} # 1 ngày (ms)

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  appointment:
    min-hours-before-booking: ${APPOINTMENT_MIN_HOURS_BEFORE_BOOKING:2}
    min-hours-before-cancellation: ${APPOINTMENT_MIN_HOURS_BEFORE_CANCELLATION:2}
    max-active-per-patient: ${APPOINTMENT_MAX_ACTIVE_PER_PATIENT:3}
    max-created-per-patient-per-day: ${APPOINTMENT_MAX_CREATED_PER_PATIENT_PER_DAY:3}

# Cấu hình Swagger/OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
